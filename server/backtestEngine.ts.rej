--- server/backtestEngine.ts
+++ server/backtestEngine.ts
@@ -546,6 +551,7 @@ export async function runBacktest(config: BacktestConfig): Promise<BacktestResu
     };
 
     // 获取所有股票的K线数据
+    progressTracker.report("fetching_candles", "正在获取K线数据", { stocks: stocksToTest.length });
     const allCandles = await getCandlesForStocks(stocksToTest, config.timeframes, config.startDate, config.endDate);
 
     // 逐个股票执行回测
